node_modules/
.DS_Store
*.log

# Runtime campaign state - belongs to each workspace, never committed
.teamwork/

# hook-probe output. Ignored because `cp -r .` when installing the plugin from a
# checkout would otherwise copy a previous session's probe log into the plugin
# cache, and the next plugin version would ship with stale test residue in it.
.zcode-probe/
